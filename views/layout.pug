doctype html
html
  head
    title= title
    link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.3.1/semantic.min.css')
    link(rel='stylesheet', href='/stylesheets/style.css')
    title Page Title
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    .header
      h1 The <i> Only </i> Recipe App
    .navbar
      a(href='/') Home
      a(href='/login') Log In/Sign Up 
      a(href="/allrecipes") Search
    .searchBar 
      form(action='#')
      input#searchBar(type='text' placeholder=' Search Recipes' name='searchBar')
      button#search search
      i.fa.fa-search(style='font-size: 18px;')
  body
    block content
